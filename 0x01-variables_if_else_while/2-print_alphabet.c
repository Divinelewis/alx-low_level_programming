#include <stdio.h>
#include <unistd.h>
/**
 * main - Entry point
 * putchar - to write string to the alphabet
 * Return: Always 0 (success)
 */
int main(void)
{
	int i;
	
	for (i = 0; i <= 52; i++)
	{
		charsmall = tolower(i);
		putchar(small);
	}	
	putchar(tolower(x);
	return (0);
}
