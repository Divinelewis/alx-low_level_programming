#include "main.h"
#include <stdio.h>

/**
 * _isupper - checks for uppercase characters
 * @c: the argument
 * Return: 1 if c is uppercase and o otherwise
 */

int _isupper(int c);
{
	return (c >= 'A' && c <= 'Z');
}
