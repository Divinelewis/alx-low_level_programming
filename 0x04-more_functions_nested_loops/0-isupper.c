#include "main.h"
#include <stdio.h>

/**
 * _isupper - checks for uppercase characters
 * @c: the argument
 * Return: 1 if c is uppercase and o otherwise
 */

int _isupper(int c);
{
	if (c >= '0' && c < '9')
	{
		return (1);
	}
	return (0);
}
